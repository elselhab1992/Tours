import React from "react";
import "./Loading.css";

const Loading = () => {
  return (
    <section className="loading-page">
      <h1>Loading...</h1>
    </section>
  );
};

export default Loading;
